{
  "rules": {
    "bags": {
      "$bagId": {
        ".read": true,
        ".write": true
      }
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "test": {
      ".read": true,
      ".write": true
    }
  }
}
